# MinerU PDF 转码依赖
mineru>=2.7.0

# PyTorch（必需）- MinerU 需要 torch 来运行模型
# macOS 用户：推荐安装支持 MPS 的版本（macOS 12.3+）
# 如果使用 MPS，安装命令：pip install torch torchvision torchaudio
# CPU 版本：pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=1.9.0

# transformers 版本要求：>=4.28.0, <5.0.0
# transformers 5.0+ 移除了 find_pruneable_heads_and_indices，导致 MinerU 无法工作
# 使用 4.x 的最新版本（推荐 4.40.0 或 4.45.0）
transformers>=4.28.0,<5.0.0

# huggingface-hub 版本要求
# transformers 4.x 需要 huggingface-hub < 1.0
huggingface-hub>=0.34.0,<1.0

# DocLayout-YOLO - MinerU 的布局分析模型（必需）
# 注意：包名是 doclayout-yolo（连字符），但导入时使用 doclayout_yolo（下划线）
doclayout-yolo>=0.0.4

# Langextract - 信息提取
langextract>=1.1.0
# 知识图谱依赖
neo4j>=2025.10
chromadb>=1.4.0

# 工具库
python-dotenv>=1.0.0
requests>=2.31.0
