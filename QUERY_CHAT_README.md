# GraphRAG Neo4j 对话查询系统使用说明

基于 GraphRAG（Graph Retrieval-Augmented Generation）的 Neo4j 知识图谱对话查询系统。

## 功能特性

- 🧠 **GraphRAG 架构**：结合图查询和向量检索，提供更准确的答案
- 🔍 **实体提取**：自动从问题中提取关键实体（添加剂、食品分类、功能等）
- 📊 **图结构查询**：在 Neo4j 中查询实体及其邻居节点和关系
- 💾 **向量检索**：使用 ChromaDB 存储和检索历史查询结果
- 🤖 **智能回答**：基于图查询结果和向量上下文生成专业回答
- 💬 **对话式交互**：支持多轮对话，保留上下文

## GraphRAG 工作流程

1. **实体提取**：使用 LLM 从用户问题中提取关键实体
2. **图查询**：在 Neo4j 中查找实体及其相关节点和关系
3. **向量检索**：从 ChromaDB 中检索相关的历史查询结果
4. **增强生成**：结合图查询结果和向量上下文，生成准确回答
5. **结果存储**：将图查询结果存储到向量数据库供后续检索

## 安装依赖

```bash
pip install chromadb requests
# 或使用 uv
uv pip install chromadb requests
```

## 配置

确保 `.env` 文件中已配置：

```env
# Neo4j 配置
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password

# Qwen API 配置
QWEN_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_API_KEY=your_api_key
```

## 使用方法

### 1. 交互式对话模式

直接运行脚本，进入对话循环：

```bash
python query_neo4j_chat.py
# 或
uv run python query_neo4j_chat.py
```

示例对话：

```
❓ 您的问题: 山梨酸能在哪些食品中使用？

🔍 正在分析问题...
📌 识别到实体: {"chemicals": ["山梨酸"], "food_categories": [], "functions": [], "codes": []}
🔎 正在查询知识图谱...
🔍 正在检索相关历史信息...
💬 正在生成回答...

📊 回答:
根据图查询结果，山梨酸及其钾盐可以在以下食品中使用：

1. 干酪、再制干酪、干酪制品及干酪类似品（01.06）：最大使用量 1.0 g/kg（以山梨酸计）
2. 氢化植物油（02.01.01.02）：最大使用量 1.0 g/kg（以山梨酸计）
3. 人造黄油（02.02.01.02）：最大使用量 1.0 g/kg（以山梨酸计）
...
```

### 2. 单次查询模式

通过命令行参数直接查询：

```bash
python query_neo4j_chat.py "查询所有着色剂"
python query_neo4j_chat.py "01.01.03 这个食品分类能用哪些添加剂？"
python query_neo4j_chat.py "CNS号 17.003 是什么添加剂？"
```

## 支持的查询类型

### 1. 查询添加剂信息
- "山梨酸是什么？"
- "查询山梨酸的信息"
- "CNS号 17.003 是什么？"

### 2. 查询使用范围
- "山梨酸能在哪些食品中使用？"
- "山梨酸的使用范围是什么？"
- "哪些食品可以使用山梨酸？"

### 3. 查询食品可用添加剂
- "01.01.03 这个食品分类能用哪些添加剂？"
- "调制乳可以用哪些添加剂？"
- "糖果类食品允许使用的添加剂有哪些？"

### 4. 按功能查询
- "查询所有着色剂"
- "有哪些增稠剂？"
- "列出所有抗氧化剂"

### 5. 复合查询
- "山梨酸在糖果中的最大使用量是多少？"
- "哪些着色剂可以在糕点上彩装中使用？"

## GraphRAG vs 传统方法

### 传统方法（直接生成 Cypher）
- ❌ 依赖 LLM 准确生成 Cypher
- ❌ 无法利用历史查询结果
- ❌ 对复杂查询可能生成错误语句

### GraphRAG 方法
- ✅ 先提取实体，再查询图结构
- ✅ 结合向量检索利用历史信息
- ✅ 基于实际图数据生成回答，更准确
- ✅ 可以处理模糊查询和复杂问题

## 向量数据库

系统会自动在 `chroma_graphrag/` 目录下创建 ChromaDB 数据库，存储：
- 图查询结果摘要
- 查询问题
- 元数据（查询时间等）

这些信息会被用于后续查询的向量检索，提高回答质量。

## 对话命令

- `quit` / `exit` / `q`：退出程序
- `clear`：清空对话历史

## 注意事项

1. **API 配置**：确保 Qwen API Key 有效且有足够额度
2. **Neo4j 连接**：确保 Neo4j 数据库正在运行且可访问
3. **数据完整性**：确保已导入完整的食品添加剂数据
4. **向量数据库**：首次运行会自动创建，后续会复用已有数据

## 故障排除

### 1. Qwen API 调用失败
- 检查 `.env` 中的 `QWEN_API_KEY` 是否正确
- 检查网络连接和 API 额度

### 2. Neo4j 连接失败
- 确认 Neo4j 服务正在运行：`neo4j status`
- 检查 `.env` 中的连接信息是否正确

### 3. ChromaDB 初始化失败
- 检查是否有写入权限
- 如果出现问题，可以删除 `chroma_graphrag/` 目录重新初始化

### 4. 实体提取不准确
- 尝试使用更明确的问题表述
- 系统会自动回退到关键词匹配

## 示例查询

```bash
# 查询添加剂
python query_neo4j_chat.py "山梨酸"

# 查询使用范围
python query_neo4j_chat.py "山梨酸能在哪些食品中使用？"

# 查询功能
python query_neo4j_chat.py "查询所有着色剂"

# 查询食品可用添加剂
python query_neo4j_chat.py "调制乳可以用哪些添加剂？"

# 复合查询
python query_neo4j_chat.py "山梨酸在糖果中的最大使用量是多少？"
```

## 技术架构

```
用户问题
    ↓
实体提取（LLM）
    ↓
图查询（Neo4j） ←→ 向量检索（ChromaDB）
    ↓
上下文合并
    ↓
答案生成（LLM）
    ↓
结果存储（ChromaDB）
```
